# Chart Components

This directory contains chart visualization components:

- `ActivityTimelineChart.jsx` - Custom canvas chart for today's activity
- `TimelineChart.jsx` - Custom canvas chart for order timeline
- `StatsChart.jsx` - Chart.js line chart for statistics
- `ProfitabilityChart.jsx` - Chart.js line chart for profitability
- Chart utilities and theme helpers
