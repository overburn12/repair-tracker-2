# Layout Components

This directory contains layout-related components:

- `App.jsx` - Root component with router setup
- `Layout.jsx` - Base layout wrapper with navbar and modals
- `Navbar.jsx` - Top navigation bar (fixed position)
  - Includes: BTC price, logo, workbook button, search, assignee select, theme toggle, settings link
