# Modal Components

This directory contains modal/overlay components:

- `WorkbookModal.jsx` - Floating workbook overlay with repair units grouped by order
- Modal sub-components for header, content, overlay
